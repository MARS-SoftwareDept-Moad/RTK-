class Employee {
  /// Creates the employee class with required details.
  Employee(this.id, this.time, this.packet);

  /// Id of an employee.
  final int id;

  /// Name of an employee.
  final DateTime time;

  /// Designation of an employee.
  final String packet;
}